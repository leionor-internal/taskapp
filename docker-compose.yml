version: '3.8'

services:
  task-tracker:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: task-tracker

    ports:
      - "3000:3000"

    restart: unless-stopped

    environment:
      NODE_ENV: production
      PORT: 3000
